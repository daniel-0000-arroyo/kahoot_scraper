name: Kahoot Scraper Auto

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Scrape desde Render
        run: |
          curl https://kahoot-scraper.onrender.com/scrape_search/math
